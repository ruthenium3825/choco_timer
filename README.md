# チョコットタイマー

チョコットランド用の非公式軽量オーバーレイタイマーです。  
ゲーム画面上にタイマーやカウンターを重ねて表示し、戦闘や周回時の時間管理を補助します。
<br>
<br>

---

## 💡 主な機能

- **タイマー機能**:  
  設定した時間をカウントダウン表示し、戦闘・周回・バフ管理などに利用できます。

- **複数タイマー管理**:  
  複数のタイマーを同時に保持でき、異なる時間管理をまとめて行えます。

- **カウンター機能**:  
  ホットキー操作で2つのカウンターを増減・リセットでき、討伐数や周回数の記録に使用できます。

- **最前面オーバーレイ表示**:  
  ゲーム画面上へタイマーを重ねて表示し、プレイ中でも確認できます。

- **ホットキー操作**:  
  タイマー開始・リセット・カウンター操作などをキーボードから直接実行できます。キー割り当て変更にも対応しています。

- **自動タイマースタート**:  
  画面上の特定アイコンを検出すると、自動でタイマーを開始できます。

- **システムトレイ常駐**:  
  システムトレイから設定変更や終了操作を行えます。
<br>

---

## 💻 動作環境

- **OS**: Windows 10 / 11 (64bit)
- **対象ゲーム**: チョコットランド (PC版)
<br>

---

## 📥 ダウンロード・起動方法

1. [👉 こちらの最新リリースページ](https://github.com/ruthenium3825/choco_timer/releases/latest) をクリックしてダウンロード画面を開きます。
2. 画面内にある **`Choco-Timer-v1.0.0.zip`** という名前のリンクをクリックすると、ダウンロードが始まります。
3. ダウンロードしたZIPファイルを、必ず右クリックして **「すべて展開（解凍）」** してください。
4. 展開したフォルダ内の **`チョコットタイマー.exe`** ファイルを起動します。
5. 起動後、システムトレイにアイコンが表示されます。ゲーム画面をアクティブにした状態で各種操作が可能です。

※ 初回起動時に「WindowsによってPCが保護されました」と警告画面が出た場合は、 **「詳細情報」** をクリックした後に表示される **「実行」** ボタンを押すと起動できます（個人開発アプリ特有の仕様です）。  
※ 使用しているセキュリティソフトによって起動がブロックされる場合は、アプリまたはフォルダを「スキャン除外（監視対象外）」に設定してください。  
<br>

---

## 🛠️ 使い方

### 1. 基本操作
1. アプリ起動後、ゲーム画面をアクティブにすると自動連動します。
2. ゲーム中に **`T` キー** を押すと、画面にタイマーが出現してカウントダウンが始まります。
3. **`R` キー** を押すと、選択されているタイマーを消去します。
4. **`.` キー** / **`,` キー** でタイマーの時間を選択できます。
5. アプリを終了する場合は、システムトレイからアイコンを**右クリック**し「終了」を押してください。（ゲームが起動されていない場合、5分経過で自動的に終了します）
<br>

* **配置**: ゲーム画面の位置やサイズに自動で追従します。ゲームを最小化、非アクティブ状態にしている間は自動的に非表示になります。
* **自動タイマースタート**: 特殊羽を使用した際に効果時間に合ったタイマーが自動で開始されます。同アイコンの移行（リア→リエなど）は検出できないため、効果時間終了後（アイコン消滅後）か `R` キー でタイマーを消去してリセットしてください。
* **文字入力中の誤作動防止**: `Enter` キーを押して文字入力状態になると、画面にキーボードアイコンが表示されてホットキーが一時無効化されます。文字入力が終わったら左クリックすると通常モードに戻ります。
* **設定の自動保存**: 変更した秒数やキーは同じフォルダ内の `config.json` に自動保存されます。アプリ（.exe）を移動する際は、このファイルも一緒に移動してください。アプリ更新時は`config.json`と同じ場所に置くだけで設定を引き継げます。

### 2. 初期ショートカットキー一覧
ゲームをプレイしたまま、以下のキーで操作できます。

| 操作内容 | デフォルトキー | 説明 |
| :--- | :--- | :--- |
| **タイマー追加** | `T` | 選択されているプリセット時間のタイマーを新しく1個追加します（最大8個）。 |
| **時間設定（進む/戻る）** | `.` (ピリオド) / `,` (カンマ) | 追加するタイマーの時間を切り替えます（画面に予告表示されます）。 |
| **リセットカーソル移動** | `Tab` | 消去したいタイマーを選択するカーソルを移動します。 |
| **選択中のタイマー消去** | `R` | カーソルが指しているタイマーを個別で1つ消去します。 |
| **全タイマーリセット** | `Ctrl + R` | 動いているすべてのタイマーを一括で強制終了します。 |
| **カウンター1 [+1 / -1]** | `Z` / `Shift + Z` | 1つ目のカウンターを増減します。 |
| **カウンター1 リセット** | `Ctrl + Z` | 1つ目のカウンター数値を `0` にリセットします。 |
| **カウンター2 [+1 / -1]** | `X` / `Shift + X` | 2つ目のカウンターを増減します。 |
| **カウンター2 リセット** | `Ctrl + X` | 2つ目のカウンター数値を `0` にリセットします。 |

### 3. 設定の変更方法
画面右下のシステムトレイにある「チョコットタイマー」のアイコンを**右クリック**すると、メニューから以下のカスタマイズが可能です。

* **ショートカットキー設定**: 各機能の割り当てキーを自由に変更できます。
* **タイマープリセット編集**: `T`キーで呼び出すデフォルトの時間を、使いやすい秒数に編集・追加できます。
* **表示サイズ・位置変更**: UI表示サイズを「標準」「大」、表示位置を「標準(上)」「やや下」から選べます。
* **各種機能のオン/オフ**: カウンター表示の非表示化や、効果音の音量調整（0〜100%）などがいつでも切り替え可能です。
<br>

---

## 🔄 アップデート方法

- 新しいバージョンが公開された場合は、同様に「Releases」から最新のバージョンをダウンロードしてください。
- 設定を引き継ぐ場合は、現在お使いの古いフォルダ内にある **`config.json`**（設定ファイル）をコピーし、新しい **`チョコットタイマー.exe`** と同じフォルダに貼り付け（移行）してください。
<br><br>

---

## ⚠️ 免責事項

- 本アプリは個人が趣味で開発した非公式のアプリです。仕様を理解した上で、自己責任でのご利用をお願いいたします。
- 本アプリの利用に伴うトラブルや不具合等について、開発者は一切の責任を負いません。
- 『チョコットランド』は、ココネ株式会社の商標です。『チョコットランド』に関わる著作権その他一切の知的財産権はココネ株式会社に属しており、このアプリはココネ株式会社とは、一切関係がありません。該当画像の転載、配布は禁止しています。
